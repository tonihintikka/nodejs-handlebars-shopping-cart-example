{{# each products}}

<div class="row">
   {{# each this }}
    <div class="col-sm-6 col-md4">
<div class="card" style="width: 18rem;">
  <img src="{{ this.imagePath }}" class="card-img-top img-responsive" alt="{{ this.title }}">
  <div class="card-body">
    <h5 class="card-title">{{ this.title }}</h5>
    <p class="card-text">{{ this.description }}</p>
    <div class="price pull-left">{{ this.price }} €</div>
    <div class="pull-right">
    <a href="#" class="btn btn-success">Add to Shopping Cart</a>
    </div>
  </div>
</div>
</div>
{{/each}}
</div>



{{/each}}


